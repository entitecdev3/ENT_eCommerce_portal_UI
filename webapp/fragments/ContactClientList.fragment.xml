<core:FragmentDefinition xmlns="sap.m" xmlns:f="sap.ui.layout.form" xmlns:core="sap.ui.core">
    <Table id="idContactClientList"
  
    items="{
        path: 'appView>/ClientDetails/ContactEmployees'
    }">

    <columns>
        <Column>
            <Text text="{i18n>id}" />
        </Column>
        <Column
            minScreenWidth="Tablet"
            demandPopin="true">
            <Text text="{i18n>name}" />
        </Column>
        <Column
            minScreenWidth="Tablet"
            demandPopin="true">
            <Text text="{i18n>lastName}" />
        </Column>
        <Column
            minScreenWidth="Tablet"
            demandPopin="true">
            <Text text="{i18n>phone}" />
        </Column>
        <Column>
            <Text text="{i18n>cellPhone}" />
        </Column>
        <Column>
            <Text text="{i18n>email}" />
        </Column>
    </columns>
    <items>
        <ColumnListItem vAlign="Middle">
            <cells>
                <ObjectIdentifier
                    text="{appView>Name}"/>
                <Text
                    text="{appView>FirstName}" />
                <Text
                    text="{appView>LastName}" />
                <Text
                    text="{appView>Phone1}" />
                <Text
                    text="{appView>MobilePhone}" />
                <Text
                    text="{appView>E_Mail}" />
               
            </cells>
        </ColumnListItem>
    </items>
</Table>
</core:FragmentDefinition>
